{
  "name": "pagefind-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "index": "pagefind --site dist",
    "serve": "pagefind --site dist --serve",
    "clean": "rm -rf dist/_pagefind"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
